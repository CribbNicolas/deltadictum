export const MEMORIES = [
  { key: 'payments/retry/idempotency', trigger: 'when retrying payment requests', trigger_variants: ['resubmit a timed out charge'],
    behavior_delta: 'Reuse the original idempotency key.', why: 'The same logical payment must not be charged twice.',
    applies_to: { components: ['payments'] }, action: 'reuse_key' },
  { key: 'data/migrations/immutable', trigger: 'when editing database migrations', trigger_variants: ['alter an applied migration'],
    behavior_delta: 'Create a new migration; preserve applied migrations.', why: 'Existing environments have already applied the earlier migration.',
    assumptions: [{ key: 'migration.applied', equals: true, description: 'the migration has already been applied' }], action: 'new_migration' },
  { key: 'tests/clock/injection', trigger: 'when testing time dependent code', trigger_variants: ['flaky expiration tests', 'pruebas de expiracion'],
    behavior_delta: 'Inject a clock into the service.', why: 'Tests must control time without sleeps.', applies_to: { files: ['src/services/**'] }, action: 'inject_clock' },
  { key: 'storage/local/sqlite', memory_type: 'decision', trigger: 'when choosing memory storage', trigger_variants: ['persist project knowledge'],
    behavior_delta: 'Use the existing SQLite index.', why: 'The plugin operates locally with one writer.',
    revisit_when: [{ kind: 'fact_changed', key: 'writers', equals: 1, description: 'multiple remote writers are required' }], action: 'sqlite' },
  { key: 'security/logging/credentials', memory_type: 'anti_memory', trigger: 'when logging authentication failures', trigger_variants: ['debug login failures', 'depurar fallos de autenticacion'],
    behavior_delta: 'Do not log credentials; log the request ID.', why: 'Authentication logs must not disclose secrets.', action: 'request_id' },
  { key: 'webhooks/dedupe/delivery', trigger: 'when processing webhook deliveries', trigger_variants: ['handle a redelivered webhook'],
    behavior_delta: 'Deduplicate by delivery_id within this provider.', why: 'Several deliveries can share an event type.',
    assumptions: [{ key: 'provider', equals: 'alpha', description: 'provider is alpha' }], action: 'delivery_id' },
  // Shaped like real stores (2026-09-22): authored operations the hooks never
  // send, and advice longer than one sentence.
  { key: 'architecture/core/layers', memory_type: 'decision', trigger: 'when adding game logic, commands or presentation',
    behavior_delta: 'Keep rules and invariants in domain, transactions and views in application, saves and catalogues in infrastructure, and engine input and rendering in client; the UI sends commands and reads views, never mutable state.',
    why: 'Simulation must stay testable without the engine.', applies_to: { files: ['src/domain/**', 'src/application/**', 'src/client/**'], operations: ['implement', 'review'] }, action: 'layers' },
  { key: 'build/export/sequential', memory_type: 'procedure', trigger: 'when exporting release builds for windows and android',
    behavior_delta: 'Export one platform at a time with the export scripts and the main executable, holding the shared lock; after the Android export restore the editor target in locked mode, because project.assets.json becomes Android-specific and the next Windows build would otherwise fail to restore.',
    why: 'Parallel exports corrupted the shared restore state twice.', action: 'sequential_export' },
  { key: 'legacy/cache/retired', trigger: 'when caching product data', behavior_delta: 'Cache forever.', why: 'Historical behavior.', lifecycle_state: 'superseded', action: 'cache_forever' },
];

export const CASES = [
  { id: 'payment-exact', action: 'when retrying payment requests', components: ['payments'], expected: ['payments/retry/idempotency'], decision: 'reuse_key' },
  { id: 'payment-paraphrase', action: 'resubmit a timed out charge', components: ['payments'], expected: ['payments/retry/idempotency'], decision: 'reuse_key' },
  { id: 'payment-spanish', action: 'reintentar solicitudes de pago', components: ['payments'], expected: ['payments/retry/idempotency'], decision: 'reuse_key' },
  { id: 'payment-other-component', action: 'retry payment requests', components: ['shipping'], expected: [], decision: 'inspect_project' },
  { id: 'migration-exact', action: 'when editing database migrations', facts: { 'migration.applied': true }, expected: ['data/migrations/immutable'], decision: 'new_migration' },
  { id: 'migration-paraphrase', action: 'alter an applied migration', facts: { 'migration.applied': true }, expected: ['data/migrations/immutable'], decision: 'new_migration' },
  { id: 'migration-spanish', action: 'editar migraciones de datos', facts: { 'migration.applied': true }, expected: ['data/migrations/immutable'], decision: 'new_migration' },
  { id: 'migration-unapplied', action: 'edit database migrations', facts: { 'migration.applied': false }, expected: [], decision: 'inspect_project' },
  { id: 'clock-exact', action: 'when testing time dependent code', files: ['src/services/expiry.js'], expected: ['tests/clock/injection'], decision: 'inject_clock' },
  { id: 'clock-paraphrase', action: 'flaky expiration tests', files: ['src/services/expiry.js'], expected: ['tests/clock/injection'], decision: 'inject_clock' },
  { id: 'clock-spanish', action: 'pruebas de expiracion', files: ['src/services/expiry.js'], expected: ['tests/clock/injection'], decision: 'inject_clock' },
  { id: 'clock-wrong-scope', action: 'flaky expiration tests', files: ['src/ui/clock.js'], expected: [], decision: 'inspect_project' },
  { id: 'storage-single', action: 'choose memory storage', facts: { writers: 1 }, expected: ['storage/local/sqlite'], decision: 'sqlite' },
  { id: 'storage-context-change', action: 'choose memory storage', facts: { writers: 3 }, expected: ['storage/local/sqlite'], review: true, decision: 'review_decision' },
  { id: 'logging-exact', action: 'when logging authentication failures', expected: ['security/logging/credentials'], decision: 'request_id' },
  { id: 'logging-spanish', action: 'depurar fallos de autenticacion', expected: ['security/logging/credentials'], decision: 'request_id' },
  { id: 'webhook-exact', action: 'when processing webhook deliveries', facts: { provider: 'alpha' }, expected: ['webhooks/dedupe/delivery'], decision: 'delivery_id' },
  { id: 'webhook-paraphrase', action: 'handle a redelivered webhook', facts: { provider: 'alpha' }, expected: ['webhooks/dedupe/delivery'], decision: 'delivery_id' },
  { id: 'webhook-other-provider', action: 'when processing webhook deliveries', facts: { provider: 'beta' }, expected: [], decision: 'inspect_project' },
  { id: 'layers-edit-tool-call', action: 'Edit {"file_path":"src/domain/combat/Resolver.cs","old_string":"var roll"}', files: ['src/domain/combat/Resolver.cs'], operation: 'edit',
    expected: ['architecture/core/layers'], contains: 'never mutable state', decision: 'layers' },
  { id: 'layers-read-tool-call', action: 'Read {"file_path":"src/client/shell/GameShell.cs"}', files: ['src/client/shell/GameShell.cs'], operation: 'read',
    expected: ['architecture/core/layers'], contains: 'never mutable state', decision: 'layers' },
  { id: 'layers-outside-scope', action: 'Edit {"file_path":"tools/build.ps1"}', files: ['tools/build.ps1'], operation: 'edit', expected: [], decision: 'inspect_project' },
  { id: 'export-long-procedure', action: 'export release builds for windows and android', expected: ['build/export/sequential'],
    contains: 'restore the editor target in locked mode', decision: 'sequential_export' },
  { id: 'retired-advice', action: 'when caching product data', expected: [], decision: 'inspect_project' },
  { id: 'unrelated-css', action: 'adjust button border radius', expected: [], decision: 'inspect_project' },
  { id: 'unrelated-readme', action: 'translate contribution instructions', expected: [], decision: 'inspect_project' },
  { id: 'unrelated-tests', action: 'write a parser smoke test', files: ['src/parser.js'], expected: [], decision: 'inspect_project' },
  { id: 'unrelated-build', action: 'package desktop assets', expected: [], decision: 'inspect_project' },
];
