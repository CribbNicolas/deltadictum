{
  "id": "6d5ce29a-26fa-4c9b-8755-1e377c185c0c",
  "project_id": "supermem",
  "schema_version": 7,
  "capture_origin": "model_initiated",
  "capture_source": "agent",
  "memory_type": "lesson",
  "scope": "project",
  "title": "Changing a CHECK in schema.sql does not reach existing indexes",
  "topic_key": "store/index/schema-check-migration",
  "trigger": "when changing a CHECK constraint or column definition of an existing table in src/store/schema.sql (for example adding a lifecycle state)",
  "behavior_delta": "Add an explicit migration in migrate() (src/store/sqlite-index.js): detect the old definition in sqlite_master, drop that table's named indexes, rename the table, exec the schema, copy the rows back and drop the old table. Bumping INDEX_FORMAT alone does not help: rebuild() deletes rows but keeps the old table and its old CHECK, and copying rows keeps activation_count, which lives only in the index.",
  "what": "Add an explicit migration in migrate() (src/store/sqlite-index.js): detect the old definition in sqlite_master, drop that table's named indexes, rename the table, exec the schema, copy the rows back and drop the old table. Bumping INDEX_FORMAT alone does not help: rebuild() deletes rows but keeps the old table and its old CHECK, and copying rows keeps activation_count, which lives only in the index.",
  "why": "migrate() runs the schema with CREATE TABLE IF NOT EXISTS, which never alters a table that already exists, so an index built by an earlier release keeps rejecting the new value at insert time. Found while planning the legacy state on 2026-09-24.",
  "tags": [],
  "trigger_variants": [],
  "applies_to": {
    "files": [
      "src/store/schema.sql",
      "src/store/sqlite-index.js",
      "src/store/create-store.js"
    ],
    "components": [
      "store"
    ],
    "operations": [
      "edit"
    ]
  },
  "assumptions": [],
  "revisit_when": [],
  "alternatives": [],
  "evidence_refs": [
    {
      "source_type": "file",
      "source_ref": "src/store/sqlite-index.js",
      "summary": "migrate() only execs schema.sql; rebuild() deletes rows from memory_atoms without recreating it."
    },
    {
      "source_type": "file",
      "source_ref": "src/store/schema.sql",
      "summary": "memory_atoms uses CREATE TABLE IF NOT EXISTS with a lifecycle_state CHECK list."
    }
  ],
  "authority": "validated",
  "lifecycle_state": "active",
  "valid_from": "2026-09-24T15:40:21.025Z",
  "valid_until": null,
  "retrieval_forms": {
    "micro": "Add an explicit migration in migrate() (src/store/sqlite-index.js): detect the old definition in sqlite_master, drop that table's named indexes, rename the table, exec the schema, copy the rows back and drop the old table. Bumping INDEX_FORMAT alone does not help: rebuild() deletes rows but keeps the old table and its old CHECK, and copying rows keeps activation_count, which lives only in the index.",
    "short": "Add an explicit migration in migrate() (src/store/sqlite-index.js): detect the old definition in sqlite_master, drop that table's named indexes, rename the table, exec the schema, copy the rows back and drop the old table. Bumping INDEX_FORMAT alone does not help: rebuild() deletes rows but keeps the old table and its old CHECK, and copying rows keeps activation_count, which lives only in the index. Why: migrate() runs the schema with CREATE TABLE IF NOT EXISTS, which never alters a table that already exists, so an index built by an earlier release keeps rejecting the new value at insert time. Found while planning the legacy state on 2026-09-24.",
    "full": "Add an explicit migration in migrate() (src/store/sqlite-index.js): detect the old definition in sqlite_master, drop that table's named indexes, rename the table, exec the schema, copy the rows back and drop the old table. Bumping INDEX_FORMAT alone does not help: rebuild() deletes rows but keeps the old table and its old CHECK, and copying rows keeps activation_count, which lives only in the index. Why: migrate() runs the schema with CREATE TABLE IF NOT EXISTS, which never alters a table that already exists, so an index built by an earlier release keeps rejecting the new value at insert time. Found while planning the legacy state on 2026-09-24."
  },
  "confidence": 0.765,
  "registry_key_id": "85033668-7954-4e6b-b0c9-f8c383e3d6e1",
  "evidence_state": {
    "checked_at": "2026-09-24T16:49:24.438Z",
    "artifacts": [
      {
        "source_type": "file",
        "source_ref": "src/store/sqlite-index.js",
        "summary": "migrate() only execs schema.sql; rebuild() deletes rows from memory_atoms without recreating it.",
        "status": "verified",
        "provenance": "filesystem",
        "hash": "a22752ddece33048adafd39558d57b7803f710212e830105a9ca9b6e0b4c9a34"
      },
      {
        "source_type": "file",
        "source_ref": "src/store/schema.sql",
        "summary": "memory_atoms uses CREATE TABLE IF NOT EXISTS with a lifecycle_state CHECK list.",
        "status": "verified",
        "provenance": "filesystem",
        "hash": "69a3ae110adeea0dba9df5c47ab96c155ed3f1b59957dc6e805582a0d24a3c57"
      }
    ],
    "verified_count": 2,
    "support": "human_reviewed"
  },
  "created_at": "2026-09-24T15:40:21.070Z",
  "updated_at": "2026-09-24T16:49:24.442Z",
  "activation_count": 0,
  "review": {
    "source": "local_ui",
    "reviewed_at": "2026-09-24T16:49:24.438Z",
    "rationale": "Auto-accepted: confidence 0.765 >= threshold 0.765."
  }
}
