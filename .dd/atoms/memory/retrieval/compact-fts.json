{
  "id": "0775e2e0-a142-443d-9615-a90c9e05bd9c",
  "project_id": "supermem",
  "schema_version": 7,
  "capture_origin": "model_initiated",
  "capture_source": "agent",
  "memory_type": "lesson",
  "scope": "project",
  "title": "Return compact micro/short hits built from FTS candidates, capped at 8 memories per response, gated ",
  "topic_key": "memory/retrieval/compact-fts",
  "trigger": "when calling the DD retrieve/orient tools, or changing retrieveMemories in src/engine/retrieve.js",
  "behavior_delta": "Return compact micro/short hits built from FTS candidates, capped at 8 memories per response, gated by a value-per-token threshold (default 0.02, config vpt_threshold). Use the `get` tool only when full evidence/relations for one specific memory are needed. Do not name a specific internal function (e.g. a helper inside retrieve.js) in this memory's content — describe the externally-observable contract instead, so a later refactor of the internal shape doesn't leave the memory citing code that no longer exists.",
  "what": "Return compact micro/short hits built from FTS candidates, capped at 8 memories per response, gated by a value-per-token threshold (default 0.02, config vpt_threshold). Use the `get` tool only when full evidence/relations for one specific memory are needed. Do not name a specific internal function (e.g. a helper inside retrieve.js) in this memory's content — describe the externally-observable contract instead, so a later refactor of the internal shape doesn't leave the memory citing code that no longer exists.",
  "why": "The existing active memory on this topic_key (b8f17c0c) is stale in two ways found during a full memory-store audit: it names a `compactHit` helper in src/engine/retrieve.js that no longer exists (retrieve.js now builds the hit object inline in the candidate loop), and it uses the pre-rename tool names `supermem_retrieve`/`supermem_get` instead of the current `orient`/`retrieve`/`get` MCP tools. Its evidence_state freshness check reported no drift because the cited file's hash hadn't changed since last verification — hash-based freshness checks provenance, not whether the memory's prose description of the code is still accurate, so this kind of staleness needs a human or an explicit audit pass to catch.",
  "tags": [],
  "trigger_variants": [],
  "applies_to": {
    "files": [
      "src/engine/retrieve.js",
      "src/mcp/tools.js"
    ],
    "components": [],
    "operations": []
  },
  "assumptions": [],
  "revisit_when": [],
  "alternatives": [],
  "evidence_refs": [
    {
      "source_type": "file",
      "source_ref": "src/engine/retrieve.js",
      "summary": "Hit objects are built inline in the candidate loop (~line 91), capped at 8 selected memories, gated by the value-per-token threshold check."
    },
    {
      "source_type": "file",
      "source_ref": "src/mcp/definition.js",
      "summary": "Current MCP tool names are orient/retrieve/get/propose/feedback/list/contradict/ui/status/health, not supermem_*."
    }
  ],
  "authority": "validated",
  "lifecycle_state": "active",
  "valid_from": "2026-09-19T10:22:01.405Z",
  "valid_until": null,
  "retrieval_forms": {
    "micro": "Return compact micro/short hits built from FTS candidates, capped at 8 memories per response, gated by a value-per-token threshold (default 0.02, config vpt_threshold). Use the `get` tool only when full evidence/relations for one specific memory are needed. Do not name a specific internal function (e.g. a helper inside retrieve.js) in this memory's content — describe the externally-observable contract instead, so a later refactor of the internal shape doesn't leave the memory citing code that no longer exists.",
    "short": "Return compact micro/short hits built from FTS candidates, capped at 8 memories per response, gated by a value-per-token threshold (default 0.02, config vpt_threshold). Use the `get` tool only when full evidence/relations for one specific memory are needed. Do not name a specific internal function (e.g. a helper inside retrieve.js) in this memory's content — describe the externally-observable contract instead, so a later refactor of the internal shape doesn't leave the memory citing code that no longer exists. Why: The existing active memory on this topic_key (b8f17c0c) is stale in two ways found during a full memory-store audit: it names a `compactHit` helper in src/engine/retrieve.js that no longer exists (retrieve.js now builds the hit object inline in the candidate loop), and it uses the pre-rename tool names `supermem_retrieve`/`supermem_get` instead of the current `orient`/`retrieve`/`get` MCP tools. Its evidence_state freshness check reported no drift because the cited file's hash hadn't changed since last verification — hash-based freshness checks provenance, not whether the memory's prose description of the code is still accurate, so this kind of staleness needs a human or an explicit audit pass to catch.",
    "full": "Return compact micro/short hits built from FTS candidates, capped at 8 memories per response, gated by a value-per-token threshold (default 0.02, config vpt_threshold). Use the `get` tool only when full evidence/relations for one specific memory are needed. Do not name a specific internal function (e.g. a helper inside retrieve.js) in this memory's content — describe the externally-observable contract instead, so a later refactor of the internal shape doesn't leave the memory citing code that no longer exists. Why: The existing active memory on this topic_key (b8f17c0c) is stale in two ways found during a full memory-store audit: it names a `compactHit` helper in src/engine/retrieve.js that no longer exists (retrieve.js now builds the hit object inline in the candidate loop), and it uses the pre-rename tool names `supermem_retrieve`/`supermem_get` instead of the current `orient`/`retrieve`/`get` MCP tools. Its evidence_state freshness check reported no drift because the cited file's hash hadn't changed since last verification — hash-based freshness checks provenance, not whether the memory's prose description of the code is still accurate, so this kind of staleness needs a human or an explicit audit pass to catch."
  },
  "confidence": 0.765,
  "registry_key_id": "31cc69c0-05cd-4cba-b211-4f1ff318d0fa",
  "evidence_state": {
    "checked_at": "2026-09-19T10:36:32.424Z",
    "artifacts": [
      {
        "source_type": "file",
        "source_ref": "src/engine/retrieve.js",
        "summary": "Hit objects are built inline in the candidate loop (~line 91), capped at 8 selected memories, gated by the value-per-token threshold check.",
        "status": "verified",
        "provenance": "filesystem",
        "hash": "8671f5d1e28c7c27059fa736aba0e3dd38bb017900de41dab7d2902ead9629bc"
      },
      {
        "source_type": "file",
        "source_ref": "src/mcp/definition.js",
        "summary": "Current MCP tool names are orient/retrieve/get/propose/feedback/list/contradict/ui/status/health, not supermem_*.",
        "status": "verified",
        "provenance": "filesystem",
        "hash": "cde3ac9db7cbaeb2977a83892ad1db919434bf1c79f01df92061fcf390e648e2"
      }
    ],
    "verified_count": 2,
    "support": "human_reviewed"
  },
  "replaces": "b8f17c0c-5752-4c83-9f14-3ee9977d2812",
  "created_at": "2026-09-19T10:22:01.410Z",
  "updated_at": "2026-09-19T10:36:32.427Z",
  "activation_count": 0,
  "review": {
    "source": "local_ui",
    "reviewed_at": "2026-09-19T10:36:32.424Z",
    "rationale": "Reviewed as proposed."
  }
}
