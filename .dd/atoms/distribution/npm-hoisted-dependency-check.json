{
  "id": "8724c2f2-f06c-42c2-a178-ec99672aa7ad",
  "project_id": "supermem",
  "schema_version": 7,
  "capture_origin": "model_initiated",
  "capture_source": "agent",
  "memory_type": "lesson",
  "scope": "project",
  "title": "Check a dependency's presence with require.resolve, not a literal node_modules path",
  "topic_key": "distribution/npm-hoisted-dependency-check",
  "trigger": "Writing or reviewing an installer/CLI script that verifies one of its own npm dependencies is present before doing work, for a package meant to be run both from a dev checkout (own local node_modules) and as an installed dependency elsewhere (npx / npm install, where npm hoists deps to the consumer's top-level node_modules).",
  "behavior_delta": "Do not check `join(pluginRoot, 'node_modules', '<pkg>', 'package.json')` with fs.access -- it only exists for a dev checkout with its own local node_modules. Use `createRequire(import.meta.url).resolve('<pkg>/package.json')` instead: it walks up through node_modules the same way the real `import` elsewhere in the codebase will resolve at runtime, so it succeeds under hoisting and still throws the same way (MODULE_NOT_FOUND) when genuinely absent.",
  "what": "Do not check `join(pluginRoot, 'node_modules', '<pkg>', 'package.json')` with fs.access -- it only exists for a dev checkout with its own local node_modules. Use `createRequire(import.meta.url).resolve('<pkg>/package.json')` instead: it walks up through node_modules the same way the real `import` elsewhere in the codebase will resolve at runtime, so it succeeds under hoisting and still throws the same way (MODULE_NOT_FOUND) when genuinely absent.",
  "why": "Found and fixed 2026-09-19 in scripts/install-codex.mjs's planCodexInstall during docs/plans/2026-09-19-harness-native-install-plan.md's own done-when verification: `npm pack` + install into a scratch project (deliberately not `npm link`, which would have hidden this by preserving a direct node_modules layout) reproduced a real ENOENT that never showed up in the dev-checkout test suite.",
  "tags": [
    "npm",
    "hoisting",
    "installer",
    "cli",
    "testing"
  ],
  "trigger_variants": [],
  "applies_to": {
    "files": [
      "scripts/install-codex.mjs"
    ],
    "components": [
      "cli",
      "installer"
    ],
    "operations": [
      "implementation",
      "review"
    ]
  },
  "assumptions": [],
  "revisit_when": [],
  "alternatives": [],
  "evidence_refs": [
    {
      "source_type": "file",
      "source_ref": "scripts/install-codex.mjs",
      "summary": "planCodexInstall's dependency-presence check, fixed from fs.access on a literal path to require.resolve."
    },
    {
      "source_type": "diff",
      "source_ref": "commit 39af05c",
      "summary": "fix: resolve the SDK dependency check through node_modules hoisting -- includes the require.resolve replacement and the commit message documenting the reproduction steps."
    }
  ],
  "authority": "validated",
  "lifecycle_state": "active",
  "valid_from": "2026-09-19T06:24:22.726Z",
  "valid_until": null,
  "retrieval_forms": {
    "micro": "Do not check `join(pluginRoot, 'node_modules', '<pkg>', 'package.json')` with fs.access -- it only exists for a dev checkout with its own local node_modules. Use `createRequire(import.meta.url).resolve('<pkg>/package.json')` instead: it walks up through node_modules the same way the real `import` elsewhere in the codebase will resolve at runtime, so it succeeds under hoisting and still throws the same way (MODULE_NOT_FOUND) when genuinely absent.",
    "short": "Do not check `join(pluginRoot, 'node_modules', '<pkg>', 'package.json')` with fs.access -- it only exists for a dev checkout with its own local node_modules. Use `createRequire(import.meta.url).resolve('<pkg>/package.json')` instead: it walks up through node_modules the same way the real `import` elsewhere in the codebase will resolve at runtime, so it succeeds under hoisting and still throws the same way (MODULE_NOT_FOUND) when genuinely absent. Why: Found and fixed 2026-09-19 in scripts/install-codex.mjs's planCodexInstall during docs/plans/2026-09-19-harness-native-install-plan.md's own done-when verification: `npm pack` + install into a scratch project (deliberately not `npm link`, which would have hidden this by preserving a direct node_modules layout) reproduced a real ENOENT that never showed up in the dev-checkout test suite.",
    "full": "Do not check `join(pluginRoot, 'node_modules', '<pkg>', 'package.json')` with fs.access -- it only exists for a dev checkout with its own local node_modules. Use `createRequire(import.meta.url).resolve('<pkg>/package.json')` instead: it walks up through node_modules the same way the real `import` elsewhere in the codebase will resolve at runtime, so it succeeds under hoisting and still throws the same way (MODULE_NOT_FOUND) when genuinely absent. Why: Found and fixed 2026-09-19 in scripts/install-codex.mjs's planCodexInstall during docs/plans/2026-09-19-harness-native-install-plan.md's own done-when verification: `npm pack` + install into a scratch project (deliberately not `npm link`, which would have hidden this by preserving a direct node_modules layout) reproduced a real ENOENT that never showed up in the dev-checkout test suite."
  },
  "confidence": 0.765,
  "registry_key_id": "7fc5abdd-799a-4e9a-a790-1d38cad70181",
  "evidence_state": {
    "checked_at": "2026-09-19T10:36:02.706Z",
    "artifacts": [
      {
        "source_type": "file",
        "source_ref": "scripts/install-codex.mjs",
        "summary": "planCodexInstall's dependency-presence check, fixed from fs.access on a literal path to require.resolve.",
        "status": "verified",
        "provenance": "filesystem",
        "hash": "eebc27c9f305d0dd950ee861a9609ec747ca17e21e7ea2410a1c4fe4fddf5728"
      },
      {
        "source_type": "diff",
        "source_ref": "commit 39af05c",
        "summary": "fix: resolve the SDK dependency check through node_modules hoisting -- includes the require.resolve replacement and the commit message documenting the reproduction steps.",
        "status": "unavailable",
        "provenance": "agent_claim"
      }
    ],
    "verified_count": 1,
    "support": "human_reviewed"
  },
  "created_at": "2026-09-19T06:24:22.817Z",
  "updated_at": "2026-09-19T10:36:02.709Z",
  "activation_count": 0,
  "review": {
    "source": "local_ui",
    "reviewed_at": "2026-09-19T10:36:02.706Z",
    "rationale": "Reviewed as proposed."
  }
}
