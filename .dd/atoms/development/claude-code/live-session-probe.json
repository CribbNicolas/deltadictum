{
  "id": "e82ba814-47e6-4401-b96e-ae01aec268cc",
  "project_id": "supermem",
  "schema_version": 7,
  "capture_origin": "model_initiated",
  "capture_source": "agent",
  "memory_type": "procedure",
  "scope": "project",
  "title": "Verify the Claude Code plugin in a real session with --plugin-dir",
  "topic_key": "development/claude-code/live-session-probe",
  "trigger": "When a change to hooks, session start or capture must be confirmed in a real Claude Code session, without installing the plugin into the user's global config",
  "behavior_delta": "Start a resident first (node src/cli.js ui as a background task) and wait until GET /api/resident reports retrieval semantic. Then, in a scratch git project, run claude -p \"<prompt>\" --plugin-dir <checkout> --allowedTools Bash --output-format stream-json --verbose < /dev/null and read hook_response events and assistant text from the JSONL. Read recorded observations from ~/.claude/plugins/data/dd-inline/<project-key>/index.sqlite (table memory_observations), and delete that directory afterwards (stop the resident first; it holds the file open).",
  "what": "Start a resident first (node src/cli.js ui as a background task) and wait until GET /api/resident reports retrieval semantic. Then, in a scratch git project, run claude -p \"<prompt>\" --plugin-dir <checkout> --allowedTools Bash --output-format stream-json --verbose < /dev/null and read hook_response events and assistant text from the JSONL. Read recorded observations from ~/.claude/plugins/data/dd-inline/<project-key>/index.sqlite (table memory_observations), and delete that directory afterwards (stop the resident first; it holds the file open).",
  "why": "--plugin-dir loads hooks/hooks.json exactly as the marketplace install does, for that run only. A resident that a hook starts inside a Bash tool call dies when the call ends, and while it loads DD is inactive: session start then omits the retrieve/propose guidance and project context, so a probe without a ready resident looks like a regression. Text output mode shows only the reply to the Stop reminder, hiding the rest.",
  "tags": [],
  "trigger_variants": [],
  "applies_to": {
    "files": [
      "hooks/hooks.json",
      "src/hooks/session-start.js",
      "src/hooks/observe.js"
    ],
    "components": [
      "hooks",
      "distribution"
    ],
    "operations": [
      "verify",
      "debug"
    ]
  },
  "assumptions": [],
  "revisit_when": [],
  "alternatives": [],
  "evidence_refs": [
    {
      "source_type": "file",
      "source_ref": "src/hooks/session-start.js",
      "summary": "An inactive resident returns only the banner and residentNotice; PULL_GUIDANCE and advisory context are added only when active."
    },
    {
      "source_type": "file",
      "source_ref": "hooks/hooks.json",
      "summary": "Plugin hooks anchored at ${CLAUDE_PLUGIN_ROOT}, including PostToolUseFailure."
    }
  ],
  "authority": "validated",
  "lifecycle_state": "active",
  "valid_from": "2026-09-24T21:56:28.210Z",
  "valid_until": null,
  "retrieval_forms": {
    "micro": "Start a resident first (node src/cli.js ui as a background task) and wait until GET /api/resident reports retrieval semantic. Then, in a scratch git project, run claude -p \"<prompt>\" --plugin-dir <checkout> --allowedTools Bash --output-format stream-json --verbose < /dev/null and read hook_response events and assistant text from the JSONL. Read recorded observations from ~/.claude/plugins/data/dd-inline/<project-key>/index.sqlite (table memory_observations), and delete that directory afterwards (stop the resident first; it holds the file open).",
    "short": "Start a resident first (node src/cli.js ui as a background task) and wait until GET /api/resident reports retrieval semantic. Then, in a scratch git project, run claude -p \"<prompt>\" --plugin-dir <checkout> --allowedTools Bash --output-format stream-json --verbose < /dev/null and read hook_response events and assistant text from the JSONL. Read recorded observations from ~/.claude/plugins/data/dd-inline/<project-key>/index.sqlite (table memory_observations), and delete that directory afterwards (stop the resident first; it holds the file open). Why: --plugin-dir loads hooks/hooks.json exactly as the marketplace install does, for that run only. A resident that a hook starts inside a Bash tool call dies when the call ends, and while it loads DD is inactive: session start then omits the retrieve/propose guidance and project context, so a probe without a ready resident looks like a regression. Text output mode shows only the reply to the Stop reminder, hiding the rest.",
    "full": "Start a resident first (node src/cli.js ui as a background task) and wait until GET /api/resident reports retrieval semantic. Then, in a scratch git project, run claude -p \"<prompt>\" --plugin-dir <checkout> --allowedTools Bash --output-format stream-json --verbose < /dev/null and read hook_response events and assistant text from the JSONL. Read recorded observations from ~/.claude/plugins/data/dd-inline/<project-key>/index.sqlite (table memory_observations), and delete that directory afterwards (stop the resident first; it holds the file open). Why: --plugin-dir loads hooks/hooks.json exactly as the marketplace install does, for that run only. A resident that a hook starts inside a Bash tool call dies when the call ends, and while it loads DD is inactive: session start then omits the retrieve/propose guidance and project context, so a probe without a ready resident looks like a regression. Text output mode shows only the reply to the Stop reminder, hiding the rest."
  },
  "confidence": 0.765,
  "registry_key_id": "96e6a47a-deeb-453b-b10b-597a7009b996",
  "evidence_state": {
    "checked_at": "2026-09-24T22:02:38.108Z",
    "artifacts": [
      {
        "source_type": "file",
        "source_ref": "src/hooks/session-start.js",
        "summary": "An inactive resident returns only the banner and residentNotice; PULL_GUIDANCE and advisory context are added only when active.",
        "status": "verified",
        "provenance": "filesystem",
        "hash": "8937610f618d0768b368825347f73b8302cc9d6e74b3db50b568d3ceb0c5bda4"
      },
      {
        "source_type": "file",
        "source_ref": "hooks/hooks.json",
        "summary": "Plugin hooks anchored at ${CLAUDE_PLUGIN_ROOT}, including PostToolUseFailure.",
        "status": "verified",
        "provenance": "filesystem",
        "hash": "db8153d7734b88de66c5ea16db597ae77885dd9a83fffa24e7d564b4ce78a6f1"
      }
    ],
    "verified_count": 2,
    "support": "human_reviewed"
  },
  "created_at": "2026-09-24T21:56:28.258Z",
  "updated_at": "2026-09-24T22:02:38.115Z",
  "activation_count": 0,
  "review": {
    "source": "local_ui",
    "reviewed_at": "2026-09-24T22:02:38.108Z",
    "rationale": "Auto-accepted: confidence 0.765 >= threshold 0.765."
  }
}
